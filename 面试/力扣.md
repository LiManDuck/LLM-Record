# LeetCode 经典面试题分类整理

## 1. 数组 (Array)

### 两数之和 (Two Sum)
**核心思路**: 使用哈希表存储已遍历的数字及其索引，一次遍历即可找到答案。

```python
def twoSum(nums, target):
    seen = {}
    for i, num in enumerate(nums):
        complement = target - num
        if complement in seen:
            return [seen[complement], i]
        seen[num] = i
```

### 买卖股票的最佳时机 (Best Time to Buy and Sell Stock)
**核心思路**: 维护最小价格和最大利润，动态更新。

```python
def maxProfit(prices):
    min_price = float('inf')
    max_profit = 0
    for price in prices:
        min_price = min(min_price, price)
        max_profit = max(max_profit, price - min_price)
    return max_profit
```

### 乘积最大子数组 (Maximum Product Subarray)
**核心思路**: 同时维护最大值和最小值（负数可能变正数），动态规划。

```python
def maxProduct(nums):
    max_prod = min_prod = result = nums[0]
    for num in nums[1:]:
        temp = max_prod
        max_prod = max(num, max_prod * num, min_prod * num)
        min_prod = min(num, temp * num, min_prod * num)
        result = max(result, max_prod)
    return result
```

### 最大子数组和 (Maximum Subarray - Kadane's Algorithm)
**核心思路**: 动态规划，当前最大和要么是自己，要么是前面的和加上自己。

```python
def maxSubArray(nums):
    max_sum = curr_sum = nums[0]
    for num in nums[1:]:
        curr_sum = max(num, curr_sum + num)
        max_sum = max(max_sum, curr_sum)
    return max_sum
```

---

## 2. 链表 (Linked List)

### 反转链表 (Reverse Linked List)
**核心思路**: 迭代法，三个指针 prev, curr, next 依次移动。

```python
def reverseList(head):
    prev = None
    curr = head
    while curr:
        next_node = curr.next
        curr.next = prev
        prev = curr
        curr = next_node
    return prev
```

### 合并两个有序链表 (Merge Two Sorted Lists)
**核心思路**: 双指针比较，使用哑节点简化操作。

```python
def mergeTwoLists(l1, l2):
    dummy = ListNode(0)
    curr = dummy
    while l1 and l2:
        if l1.val < l2.val:
            curr.next = l1
            l1 = l1.next
        else:
            curr.next = l2
            l2 = l2.next
        curr = curr.next
    curr.next = l1 or l2
    return dummy.next
```

### 环形链表 (Linked List Cycle)
**核心思路**: 快慢指针，快指针每次走两步，慢指针走一步，相遇则有环。

```python
def hasCycle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow == fast:
            return True
    return False
```

### 相交链表 (Intersection of Two Linked Lists)
**核心思路**: 双指针走完自己的链表后走对方的链表，相遇点即为交点。

```python
def getIntersectionNode(headA, headB):
    if not headA or not headB:
        return None
    pA, pB = headA, headB
    while pA != pB:
        pA = pA.next if pA else headB
        pB = pB.next if pB else headA
    return pA
```

---

## 3. 字符串 (String)

### 最长回文子串 (Longest Palindromic Substring)
**核心思路**: 中心扩展法，以每个字符为中心向两边扩展。

```python
def longestPalindrome(s):
    def expand(left, right):
        while left >= 0 and right < len(s) and s[left] == s[right]:
            left -= 1
            right += 1
        return s[left+1:right]
    
    result = ""
    for i in range(len(s)):
        odd = expand(i, i)
        even = expand(i, i+1)
        result = max(result, odd, even, key=len)
    return result
```

### 无重复字符的最长子串 (Longest Substring Without Repeating)
**核心思路**: 滑动窗口 + 哈希表记录字符位置。

```python
def lengthOfLongestSubstring(s):
    seen = {}
    left = max_len = 0
    for right, char in enumerate(s):
        if char in seen and seen[char] >= left:
            left = seen[char] + 1
        seen[char] = right
        max_len = max(max_len, right - left + 1)
    return max_len
```

### 有效的括号 (Valid Parentheses)
**核心思路**: 栈，遇到左括号压栈，遇到右括号弹栈匹配。

```python
def isValid(s):
    stack = []
    mapping = {')': '(', '}': '{', ']': '['}
    for char in s:
        if char in mapping:
            if not stack or stack.pop() != mapping[char]:
                return False
        else:
            stack.append(char)
    return not stack
```

### 字符串相乘 (Multiply Strings)
**核心思路**: 模拟乘法竖式，结果数组长度最多为 m+n。

```python
def multiply(num1, num2):
    if num1 == "0" or num2 == "0":
        return "0"
    m, n = len(num1), len(num2)
    result = [0] * (m + n)
    
    for i in range(m-1, -1, -1):
        for j in range(n-1, -1, -1):
            mul = int(num1[i]) * int(num2[j])
            p1, p2 = i + j, i + j + 1
            total = mul + result[p2]
            result[p2] = total % 10
            result[p1] += total // 10
    
    result_str = ''.join(map(str, result))
    return result_str.lstrip('0') or '0'
```

---

## 4. 树 (Tree)

### 二叉树的最大深度 (Maximum Depth of Binary Tree)
**核心思路**: 递归，深度 = 1 + max(左子树深度, 右子树深度)。

```python
def maxDepth(root):
    if not root:
        return 0
    return 1 + max(maxDepth(root.left), maxDepth(root.right))
```

### 验证二叉搜索树 (Validate Binary Search Tree)
**核心思路**: 递归验证，传递上下界限制节点值范围。

```python
def isValidBST(root, low=float('-inf'), high=float('inf')):
    if not root:
        return True
    if root.val <= low or root.val >= high:
        return False
    return (isValidBST(root.left, low, root.val) and 
            isValidBST(root.right, root.val, high))
```

### 二叉树的层序遍历 (Level Order Traversal)
**核心思路**: BFS 广度优先搜索，使用队列。

```python
def levelOrder(root):
    if not root:
        return []
    result, queue = [], [root]
    while queue:
        level = []
        for _ in range(len(queue)):
            node = queue.pop(0)
            level.append(node.val)
            if node.left:
                queue.append(node.left)
            if node.right:
                queue.append(node.right)
        result.append(level)
    return result
```

### 二叉树的最近公共祖先 (Lowest Common Ancestor)
**核心思路**: 递归，如果在左右子树分别找到p和q，当前节点就是LCA。

```python
def lowestCommonAncestor(root, p, q):
    if not root or root == p or root == q:
        return root
    left = lowestCommonAncestor(root.left, p, q)
    right = lowestCommonAncestor(root.right, p, q)
    if left and right:
        return root
    return left or right
```

### 二叉树的序列化与反序列化 (Serialize and Deserialize)
**核心思路**: 层序遍历序列化，队列辅助反序列化。

```python
def serialize(root):
    if not root:
        return ""
    queue, result = [root], []
    while queue:
        node = queue.pop(0)
        if node:
            result.append(str(node.val))
            queue.extend([node.left, node.right])
        else:
            result.append("null")
    return ",".join(result)

def deserialize(data):
    if not data:
        return None
    vals = data.split(",")
    root = TreeNode(int(vals[0]))
    queue, i = [root], 1
    while queue:
        node = queue.pop(0)
        if vals[i] != "null":
            node.left = TreeNode(int(vals[i]))
            queue.append(node.left)
        i += 1
        if vals[i] != "null":
            node.right = TreeNode(int(vals[i]))
            queue.append(node.right)
        i += 1
    return root
```

---

## 5. 动态规划 (Dynamic Programming)

### 爬楼梯 (Climbing Stairs)
**核心思路**: 斐波那契数列，f(n) = f(n-1) + f(n-2)。

```python
def climbStairs(n):
    if n <= 2:
        return n
    prev, curr = 1, 2
    for _ in range(3, n+1):
        prev, curr = curr, prev + curr
    return curr
```

### 最长公共子序列 (Longest Common Subsequence)
**核心思路**: 二维DP，dp[i][j] 表示 text1[:i] 和 text2[:j] 的LCS长度。

```python
def longestCommonSubsequence(text1, text2):
    m, n = len(text1), len(text2)
    dp = [[0] * (n+1) for _ in range(m+1)]
    for i in range(1, m+1):
        for j in range(1, n+1):
            if text1[i-1] == text2[j-1]:
                dp[i][j] = dp[i-1][j-1] + 1
            else:
                dp[i][j] = max(dp[i-1][j], dp[i][j-1])
    return dp[m][n]
```

### 零钱兑换 (Coin Change)
**核心思路**: 完全背包问题，dp[i] 表示凑成金额i的最少硬币数。

```python
def coinChange(coins, amount):
    dp = [float('inf')] * (amount + 1)
    dp[0] = 0
    for i in range(1, amount + 1):
        for coin in coins:
            if i >= coin:
                dp[i] = min(dp[i], dp[i-coin] + 1)
    return dp[amount] if dp[amount] != float('inf') else -1
```

### 最长递增子序列 (Longest Increasing Subsequence)
**核心思路**: dp[i] 表示以 nums[i] 结尾的最长递增子序列长度。

```python
def lengthOfLIS(nums):
    if not nums:
        return 0
    dp = [1] * len(nums)
    for i in range(1, len(nums)):
        for j in range(i):
            if nums[i] > nums[j]:
                dp[i] = max(dp[i], dp[j] + 1)
    return max(dp)
```

### 编辑距离 (Edit Distance)
**核心思路**: 二维DP，三种操作（插入、删除、替换）取最小值。

```python
def minDistance(word1, word2):
    m, n = len(word1), len(word2)
    dp = [[0] * (n+1) for _ in range(m+1)]
    
    for i in range(m+1):
        dp[i][0] = i
    for j in range(n+1):
        dp[0][j] = j
    
    for i in range(1, m+1):
        for j in range(1, n+1):
            if word1[i-1] == word2[j-1]:
                dp[i][j] = dp[i-1][j-1]
            else:
                dp[i][j] = min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]) + 1
    return dp[m][n]
```

---

## 6. 回溯 (Backtracking)

### 全排列 (Permutations)
**核心思路**: 递归回溯，使用 path 记录当前排列。

```python
def permute(nums):
    result = []
    def backtrack(path):
        if len(path) == len(nums):
            result.append(path[:])
            return
        for num in nums:
            if num not in path:
                path.append(num)
                backtrack(path)
                path.pop()
    backtrack([])
    return result
```

### 子集 (Subsets)
**核心思路**: 回溯，每个位置选择加入或不加入。

```python
def subsets(nums):
    result = []
    def backtrack(start, path):
        result.append(path[:])
        for i in range(start, len(nums)):
            path.append(nums[i])
            backtrack(i+1, path)
            path.pop()
    backtrack(0, [])
    return result
```

### 括号生成 (Generate Parentheses)
**核心思路**: 回溯，左括号数量 < n 可以加左括号，右括号数量 < 左括号数量可以加右括号。

```python
def generateParenthesis(n):
    result = []
    def backtrack(path, left, right):
        if len(path) == 2 * n:
            result.append(path)
            return
        if left < n:
            backtrack(path + '(', left+1, right)
        if right < left:
            backtrack(path + ')', left, right+1)
    backtrack('', 0, 0)
    return result
```

### 组合总和 (Combination Sum)
**核心思路**: 回溯 + 剪枝，数字可以重复使用。

```python
def combinationSum(candidates, target):
    result = []
    def backtrack(start, path, remain):
        if remain == 0:
            result.append(path[:])
            return
        if remain < 0:
            return
        for i in range(start, len(candidates)):
            path.append(candidates[i])
            backtrack(i, path, remain - candidates[i])
            path.pop()
    backtrack(0, [], target)
    return result
```

### N皇后 (N-Queens)
**核心思路**: 回溯，用集合记录已占用的列、对角线。

```python
def solveNQueens(n):
    result = []
    cols = set()
    diag1 = set()  # row - col
    diag2 = set()  # row + col
    
    def backtrack(row, board):
        if row == n:
            result.append([''.join(r) for r in board])
            return
        for col in range(n):
            if col in cols or (row-col) in diag1 or (row+col) in diag2:
                continue
            cols.add(col)
            diag1.add(row-col)
            diag2.add(row+col)
            board[row][col] = 'Q'
            backtrack(row+1, board)
            board[row][col] = '.'
            cols.remove(col)
            diag1.remove(row-col)
            diag2.remove(row+col)
    
    backtrack(0, [['.'] * n for _ in range(n)])
    return result
```

---

## 7. 图 (Graph)

### 岛屿数量 (Number of Islands)
**核心思路**: DFS 或 BFS，遍历到陆地就进行深度搜索并标记。

```python
def numIslands(grid):
    if not grid:
        return 0
    count = 0
    def dfs(i, j):
        if i < 0 or i >= len(grid) or j < 0 or j >= len(grid[0]) or grid[i][j] != '1':
            return
        grid[i][j] = '0'
        dfs(i+1, j)
        dfs(i-1, j)
        dfs(i, j+1)
        dfs(i, j-1)
    
    for i in range(len(grid)):
        for j in range(len(grid[0])):
            if grid[i][j] == '1':
                dfs(i, j)
                count += 1
    return count
```

### 课程表 (Course Schedule - 拓扑排序)
**核心思路**: DFS 检测有向图是否有环，三色标记法。

```python
def canFinish(numCourses, prerequisites):
    graph = [[] for _ in range(numCourses)]
    for course, pre in prerequisites:
        graph[pre].append(course)
    
    visited = [0] * numCourses  # 0: 未访问, 1: 访问中, 2: 已完成
    
    def dfs(course):
        if visited[course] == 1:
            return False  # 检测到环
        if visited[course] == 2:
            return True
        visited[course] = 1
        for next_course in graph[course]:
            if not dfs(next_course):
                return False
        visited[course] = 2
        return True
    
    return all(dfs(i) for i in range(numCourses))
```

### 克隆图 (Clone Graph)
**核心思路**: DFS/BFS + 哈希表记录已克隆节点。

```python
def cloneGraph(node):
    if not node:
        return None
    visited = {}
    def dfs(node):
        if node in visited:
            return visited[node]
        clone = Node(node.val)
        visited[node] = clone
        for neighbor in node.neighbors:
            clone.neighbors.append(dfs(neighbor))
        return clone
    return dfs(node)
```

### 单词接龙 (Word Ladder)
**核心思路**: BFS 最短路径，每次改变一个字母。

```python
def ladderLength(beginWord, endWord, wordList):
    wordSet = set(wordList)
    if endWord not in wordSet:
        return 0
    
    queue = [(beginWord, 1)]
    while queue:
        word, length = queue.pop(0)
        if word == endWord:
            return length
        for i in range(len(word)):
            for c in 'abcdefghijklmnopqrstuvwxyz':
                next_word = word[:i] + c + word[i+1:]
                if next_word in wordSet:
                    wordSet.remove(next_word)
                    queue.append((next_word, length+1))
    return 0
```

---

## 8. 二分查找 (Binary Search)

### 搜索旋转排序数组 (Search in Rotated Sorted Array)
**核心思路**: 二分查找，判断哪半边有序，再判断 target 在哪边。

```python
def search(nums, target):
    left, right = 0, len(nums) - 1
    while left <= right:
        mid = (left + right) // 2
        if nums[mid] == target:
            return mid
        if nums[left] <= nums[mid]:  # 左半部分有序
            if nums[left] <= target < nums[mid]:
                right = mid - 1
            else:
                left = mid + 1
        else:  # 右半部分有序
            if nums[mid] < target <= nums[right]:
                left = mid + 1
            else:
                right = mid - 1
    return -1
```

### 寻找峰值 (Find Peak Element)
**核心思路**: 二分查找，比较 mid 和 mid+1，向更大的一侧移动。

```python
def findPeakElement(nums):
    left, right = 0, len(nums) - 1
    while left < right:
        mid = (left + right) // 2
        if nums[mid] > nums[mid + 1]:
            right = mid
        else:
            left = mid + 1
    return left
```

### 寻找两个正序数组的中位数 (Median of Two Sorted Arrays)
**核心思路**: 二分查找，在较短数组上进行分割。

```python
def findMedianSortedArrays(nums1, nums2):
    if len(nums1) > len(nums2):
        nums1, nums2 = nums2, nums1
    m, n = len(nums1), len(nums2)
    left, right = 0, m
    
    while left <= right:
        i = (left + right) // 2
        j = (m + n + 1) // 2 - i
        
        max_left1 = float('-inf') if i == 0 else nums1[i-1]
        min_right1 = float('inf') if i == m else nums1[i]
        max_left2 = float('-inf') if j == 0 else nums2[j-1]
        min_right2 = float('inf') if j == n else nums2[j]
        
        if max_left1 <= min_right2 and max_left2 <= min_right1:
            if (m + n) % 2 == 0:
                return (max(max_left1, max_left2) + min(min_right1, min_right2)) / 2
            else:
                return max(max_left1, max_left2)
        elif max_left1 > min_right2:
            right = i - 1
        else:
            left = i + 1
```

---

## 9. 双指针 (Two Pointers)

### 三数之和 (3Sum)
**核心思路**: 排序 + 双指针，固定一个数，另外两个数用双指针查找。

```python
def threeSum(nums):
    nums.sort()
    result = []
    for i in range(len(nums) - 2):
        if i > 0 and nums[i] == nums[i-1]:
            continue
        left, right = i + 1, len(nums) - 1
        while left < right:
            total = nums[i] + nums[left] + nums[right]
            if total < 0:
                left += 1
            elif total > 0:
                right -= 1
            else:
                result.append([nums[i], nums[left], nums[right]])
                while left < right and nums[left] == nums[left+1]:
                    left += 1
                while left < right and nums[right] == nums[right-1]:
                    right -= 1
                left += 1
                right -= 1
    return result
```

### 接雨水 (Trapping Rain Water)
**核心思路**: 双指针，维护左右最大高度，较小的一侧可以确定能接的水量。

```python
def trap(height):
    left, right = 0, len(height) - 1
    left_max = right_max = water = 0
    while left < right:
        if height[left] < height[right]:
            if height[left] >= left_max:
                left_max = height[left]
            else:
                water += left_max - height[left]
            left += 1
        else:
            if height[right] >= right_max:
                right_max = height[right]
            else:
                water += right_max - height[right]
            right -= 1
    return water
```

### 盛最多水的容器 (Container With Most Water)
**核心思路**: 双指针，从两端向中间移动，每次移动较短的那一边。

```python
def maxArea(height):
    left, right = 0, len(height) - 1
    max_area = 0
    while left < right:
        area = min(height[left], height[right]) * (right - left)
        max_area = max(max_area, area)
        if height[left] < height[right]:
            left += 1
        else:
            right -= 1
    return max_area
```

---

## 10. 滑动窗口 (Sliding Window)

### 最小覆盖子串 (Minimum Window Substring)
**核心思路**: 滑动窗口 + 哈希表，右指针扩展，左指针收缩。

```python
def minWindow(s, t):
    from collections import Counter
    need = Counter(t)
    missing = len(t)
    left = start = end = 0
    
    for right, char in enumerate(s):
        if need[char] > 0:
            missing -= 1
        need[char] -= 1
        
        while missing == 0:
            if end == 0 or right - left + 1 < end - start:
                start, end = left, right + 1
            need[s[left]] += 1
            if need[s[left]] > 0:
                missing += 1
            left += 1
    
    return s[start:end]
```

### 找到字符串中所有字母异位词 (Find All Anagrams)
**核心思路**: 固定长度滑动窗口 + 哈希表计数。

```python
def findAnagrams(s, p):
    from collections import Counter
    result = []
    p_count = Counter(p)
    window_count = Counter()
    
    for i in range(len(s)):
        window_count[s[i]] += 1
        if i >= len(p):
            if window_count[s[i-len(p)]] == 1:
                del window_count[s[i-len(p)]]
            else:
                window_count[s[i-len(p)]] -= 1
        if window_count == p_count:
            result.append(i - len(p) + 1)
    
    return result
```

### 最长连续序列 (Longest Consecutive Sequence)
**核心思路**: 哈希集合，只从序列起点开始遍历。

```python
def longestConsecutive(nums):
    num_set = set(nums)
    max_len = 0
    
    for num in num_set:
        if num - 1 not in num_set:  # 只从序列起点开始
            curr = num
            curr_len = 1
            while curr + 1 in num_set:
                curr += 1
                curr_len += 1
            max_len = max(max_len, curr_len)
    
    return max_len
```

---

## 11. 栈和队列 (Stack & Queue)

### 最小栈 (Min Stack)
**核心思路**: 使用辅助栈存储每个状态下的最小值。

```python
class MinStack:
    def __init__(self):
        self.stack = []
        self.min_stack = []
    
    def push(self, val):
        self.stack.append(val)
        if not self.min_stack or val <= self.min_stack[-1]:
            self.min_stack.append(val)
    
    def pop(self):
        if self.stack.pop() == self.min_stack[-1]:
            self.min_stack.pop()
    
    def top(self):
        return self.stack[-1]
    
    def getMin(self):
        return self.min_stack[-1]
```

### 柱状图中最大的矩形 (Largest Rectangle in Histogram)
**核心思路**: 单调栈，遇到更小的高度时计算面积。

```python
def largestRectangleArea(heights):
    stack = []
    max_area = 0
    heights.append(0)  # 哨兵
    
    for i, h in enumerate(heights):
        while stack and heights[stack[-1]] > h:
            height_index = stack.pop()
            width = i if not stack else i - stack[-1] - 1
            max_area = max(max_area, heights[height_index] * width)
        stack.append(i)
    
    return max_area
```

### 每日温度 (Daily Temperatures)
**核心思路**: 单调栈，存储索引，维护递减栈。

```python
def dailyTemperatures(temperatures):
    result = [0] * len(temperatures)
    stack = []
    
    for i, temp in enumerate(temperatures):
        while stack and temperatures[stack[-1]] < temp:
            prev_index = stack.pop()
            result[prev_index] = i - prev_index
        stack.append(i)
    
    return result
```

---

## 12. 堆 (Heap)

### 数组中的第K个最大元素 (Kth Largest Element)
**核心思路**: 最小堆，维护大小为k的堆。

```python
import heapq

def findKthLargest(nums, k):
    return heapq.nlargest(k, nums)[-1]

# 或者手动维护堆
def findKthLargest(nums, k):
    heap = []
    for num in nums:
        heapq.heappush(heap, num)
        if len(heap) > k:
            heapq.heappop(heap)
    return heap[0]
```

### 前K个高频元素 (Top K Frequent Elements)
**核心思路**: 哈希表计数 + 堆。

```python
def topKFrequent(nums, k):
    from collections import Counter
    import heapq
    count = Counter(nums)
    return heapq.nlargest(k, count.keys(), key=count.get)
```

### 合并K个升序链表 (Merge K Sorted Lists)
**核心思路**: 最小堆，每次取出最小的节点。

```python
def mergeKLists(lists):
    import heapq
    heap = []
    for i, lst in enumerate(lists):
        if lst:
            heapq.heappush(heap, (lst.val, i, lst))
    
    dummy = ListNode(0)
    curr = dummy
    
    while heap:
        val, i, node = heapq.heappop(heap)
        curr.next = node
        curr = curr.next
        if node.next:
            heapq.heappush(heap, (node.next.val, i, node.next))
    
    return dummy.next
```

---

## 13. 位运算 (Bit Manipulation)

### 只出现一次的数字 (Single Number)
**核心思路**: 异或运算，相同数字异或为0。

```python
def singleNumber(nums):
    result = 0
    for num in nums:
        result ^= num
    return result
```

### 2的幂 (Power of Two)
**核心思路**: 2的幂的二进制只有一个1，n & (n-1) 消除最右边的1。

```python
def isPowerOfTwo(n):
    return n > 0 and (n & (n - 1)) == 0
```

### 比特位计数 (Counting Bits)
**核心思路**: dp[i] = dp[i >> 1] + (i & 1)

```python
def countBits(n):
    result = [0] * (n + 1)
    for i in range(1, n + 1):
        result[i] = result[i >> 1] + (i & 1)
    return result
```

---

## 14. 数学问题 (Math)

### 回文数 (Palindrome Number)
**核心思路**: 反转一半数字进行比较。

```python
def isPalindrome(x):
    if x < 0 or (x % 10 == 0 and x != 0):
        return False
    reversed_half = 0
    while x > reversed_half:
        reversed_half = reversed_half * 10 + x % 10
        x //= 10
    return x == reversed_half or x == reversed_half // 10
```

### 多数元素 (Majority Element - 摩尔投票)
**核心思路**: Boyer-Moore 投票算法。

```python
def majorityElement(nums):
    candidate = count = 0
    for num in nums:
        if count == 0:
            candidate = num
        count += (1 if num == candidate else -1)
    return candidate
```

### 阶乘后的零 (Factorial Trailing Zeroes)
**核心思路**: 计算因子5的个数。

```python
def trailingZeroes(n):
    count = 0
    while n > 0:
        n //= 5
        count += n
    return count
```

---

## 15. 贪心算法 (Greedy)

### 跳跃游戏 (Jump Game)
**核心思路**: 贪心，维护能到达的最远位置。

```python
def canJump(nums):
    max_reach = 0
    for i in range(len(nums)):
        if i > max_reach:
            return False
        max_reach = max(max_reach, i + nums[i])
    return True
```

### 跳跃游戏 II (Jump Game II)
**核心思路**: 贪心，记录当前跳跃范围和下一跳能到达的最远位置。

```python
def jump(nums):
    jumps = curr_end = max_reach = 0
    for i in range(len(nums) - 1):
        max_reach = max(max_reach, i + nums[i])
        if i == curr_end:
            jumps += 1
            curr_end = max_reach
    return jumps
```

### 加油站 (Gas Station)
**核心思路**: 贪心，总油量 >= 总消耗才可能完成，从亏空点的下一个位置开始。

```python
def canCompleteCircuit(gas, cost):
    total_tank = curr_tank = start = 0
    for i in range(len(gas)):
        total_tank += gas[i] - cost[i]
        curr_tank += gas[i] - cost[i]
        if curr_tank < 0:
            start = i + 1
            curr_tank = 0
    return start if total_tank >= 0 else -1
```

### 分发糖果 (Candy)
**核心思路**: 两次遍历，先从左到右，再从右到左。

```python
def candy(ratings):
    n = len(ratings)
    candies = [1] * n
    
    # 从左到右
    for i in range(1, n):
        if ratings[i] > ratings[i-1]:
            candies[i] = candies[i-1] + 1
    
    # 从右到左
    for i in range(n-2, -1, -1):
        if ratings[i] > ratings[i+1]:
            candies[i] = max(candies[i], candies[i+1] + 1)
    
    return sum(candies)
```

### 买卖股票的最佳时机 II (Best Time to Buy and Sell Stock II)
**核心思路**: 贪心，所有上涨的利润都累加。

```python
def maxProfit(prices):
    profit = 0
    for i in range(1, len(prices)):
        if prices[i] > prices[i-1]:
            profit += prices[i] - prices[i-1]
    return profit
```

---

## 16. 前缀和 & 差分 (Prefix Sum & Difference Array)

### 和为K的子数组 (Subarray Sum Equals K)
**核心思路**: 前缀和 + 哈希表，记录每个前缀和出现的次数。

```python
def subarraySum(nums, k):
    from collections import defaultdict
    prefix_sum = defaultdict(int)
    prefix_sum[0] = 1
    curr_sum = count = 0
    
    for num in nums:
        curr_sum += num
        count += prefix_sum[curr_sum - k]
        prefix_sum[curr_sum] += 1
    
    return count
```

### 连续的子数组和 (Continuous Subarray Sum)
**核心思路**: 前缀和 + 取模 + 哈希表，同余定理。

```python
def checkSubarraySum(nums, k):
    prefix_mod = {0: -1}
    curr_sum = 0
    
    for i, num in enumerate(nums):
        curr_sum += num
        mod = curr_sum % k
        if mod in prefix_mod:
            if i - prefix_mod[mod] >= 2:
                return True
        else:
            prefix_mod[mod] = i
    
    return False
```

### 区域和检索 - 数组不可变 (Range Sum Query - Immutable)
**核心思路**: 前缀和数组。

```python
class NumArray:
    def __init__(self, nums):
        self.prefix = [0]
        for num in nums:
            self.prefix.append(self.prefix[-1] + num)
    
    def sumRange(self, left, right):
        return self.prefix[right+1] - self.prefix[left]
```

---

## 17. 单调栈进阶 (Monotonic Stack Advanced)

### 下一个更大元素 II (Next Greater Element II)
**核心思路**: 循环数组 + 单调栈，遍历两遍。

```python
def nextGreaterElements(nums):
    n = len(nums)
    result = [-1] * n
    stack = []
    
    for i in range(2 * n):
        while stack and nums[stack[-1]] < nums[i % n]:
            result[stack.pop()] = nums[i % n]
        if i < n:
            stack.append(i)
    
    return result
```

### 移掉K位数字 (Remove K Digits)
**核心思路**: 单调栈，保持递增，移除更大的数字。

```python
def removeKdigits(num, k):
    stack = []
    for digit in num:
        while stack and k > 0 and stack[-1] > digit:
            stack.pop()
            k -= 1
        stack.append(digit)
    
    # 如果还需要删除
    if k > 0:
        stack = stack[:-k]
    
    # 去除前导零
    return ''.join(stack).lstrip('0') or '0'
```

---

## 18. 并查集 (Union Find)

### 并查集模板
**核心思路**: 路径压缩 + 按秩合并。

```python
class UnionFind:
    def __init__(self, n):
        self.parent = list(range(n))
        self.rank = [0] * n
        self.count = n
    
    def find(self, x):
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])  # 路径压缩
        return self.parent[x]
    
    def union(self, x, y):
        root_x, root_y = self.find(x), self.find(y)
        if root_x == root_y:
            return False
        
        # 按秩合并
        if self.rank[root_x] < self.rank[root_y]:
            self.parent[root_x] = root_y
        elif self.rank[root_x] > self.rank[root_y]:
            self.parent[root_y] = root_x
        else:
            self.parent[root_y] = root_x
            self.rank[root_x] += 1
        
        self.count -= 1
        return True
```

### 冗余连接 (Redundant Connection)
**核心思路**: 并查集检测环。

```python
def findRedundantConnection(edges):
    uf = UnionFind(len(edges) + 1)
    for u, v in edges:
        if not uf.union(u, v):
            return [u, v]
```

### 省份数量 (Number of Provinces)
**核心思路**: 并查集统计连通分量。

```python
def findCircleNum(isConnected):
    n = len(isConnected)
    uf = UnionFind(n)
    
    for i in range(n):
        for j in range(i+1, n):
            if isConnected[i][j]:
                uf.union(i, j)
    
    return uf.count
```

---

## 19. 字典树 (Trie)

### 实现 Trie (前缀树)
**核心思路**: 树形结构，每个节点包含子节点字典和结束标记。

```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.is_end = False

class Trie:
    def __init__(self):
        self.root = TrieNode()
    
    def insert(self, word):
        node = self.root
        for char in word:
            if char not in node.children:
                node.children[char] = TrieNode()
            node = node.children[char]
        node.is_end = True
    
    def search(self, word):
        node = self.root
        for char in word:
            if char not in node.children:
                return False
            node = node.children[char]
        return node.is_end
    
    def startsWith(self, prefix):
        node = self.root
        for char in prefix:
            if char not in node.children:
                return False
            node = node.children[char]
        return True
```

### 单词搜索 II (Word Search II)
**核心思路**: Trie + DFS 回溯。

```python
def findWords(board, words):
    # 构建 Trie
    trie = {}
    for word in words:
        node = trie
        for char in word:
            node = node.setdefault(char, {})
        node['#'] = word
    
    result = []
    m, n = len(board), len(board[0])
    
    def dfs(i, j, node):
        if '#' in node:
            result.append(node['#'])
            del node['#']  # 避免重复
        
        if i < 0 or i >= m or j < 0 or j >= n:
            return
        
        char = board[i][j]
        if char not in node:
            return
        
        board[i][j] = '*'  # 标记已访问
        for di, dj in [(0,1), (1,0), (0,-1), (-1,0)]:
            dfs(i+di, j+dj, node[char])
        board[i][j] = char  # 恢复
    
    for i in range(m):
        for j in range(n):
            dfs(i, j, trie)
    
    return result
```

---

## 20. 区间问题 (Interval)

### 合并区间 (Merge Intervals)
**核心思路**: 排序后合并重叠区间。

```python
def merge(intervals):
    intervals.sort(key=lambda x: x[0])
    merged = [intervals[0]]
    
    for interval in intervals[1:]:
        if interval[0] <= merged[-1][1]:
            merged[-1][1] = max(merged[-1][1], interval[1])
        else:
            merged.append(interval)
    
    return merged
```

### 插入区间 (Insert Interval)
**核心思路**: 分三部分处理：左侧不重叠、重叠合并、右侧不重叠。

```python
def insert(intervals, newInterval):
    result = []
    i = 0
    
    # 添加左侧不重叠的区间
    while i < len(intervals) and intervals[i][1] < newInterval[0]:
        result.append(intervals[i])
        i += 1
    
    # 合并重叠区间
    while i < len(intervals) and intervals[i][0] <= newInterval[1]:
        newInterval[0] = min(newInterval[0], intervals[i][0])
        newInterval[1] = max(newInterval[1], intervals[i][1])
        i += 1
    result.append(newInterval)
    
    # 添加右侧不重叠的区间
    while i < len(intervals):
        result.append(intervals[i])
        i += 1
    
    return result
```

### 无重叠区间 (Non-overlapping Intervals)
**核心思路**: 贪心，按结束时间排序，尽可能保留更多区间。

```python
def eraseOverlapIntervals(intervals):
    if not intervals:
        return 0
    
    intervals.sort(key=lambda x: x[1])
    count = 0
    end = intervals[0][1]
    
    for i in range(1, len(intervals)):
        if intervals[i][0] < end:
            count += 1
        else:
            end = intervals[i][1]
    
    return count
```

### 会议室 II (Meeting Rooms II)
**核心思路**: 最小堆，记录正在进行的会议的结束时间。

```python
def minMeetingRooms(intervals):
    if not intervals:
        return 0
    
    import heapq
    intervals.sort(key=lambda x: x[0])
    heap = [intervals[0][1]]
    
    for interval in intervals[1:]:
        if interval[0] >= heap[0]:
            heapq.heappop(heap)
        heapq.heappush(heap, interval[1])
    
    return len(heap)
```

---

## 21. 设计题 (Design)

### LRU缓存 (LRU Cache)
**核心思路**: 哈希表 + 双向链表。

```python
class Node:
    def __init__(self, key=0, value=0):
        self.key = key
        self.value = value
        self.prev = None
        self.next = None

class LRUCache:
    def __init__(self, capacity):
        self.capacity = capacity
        self.cache = {}
        self.head = Node()
        self.tail = Node()
        self.head.next = self.tail
        self.tail.prev = self.head
    
    def _add_to_head(self, node):
        node.prev = self.head
        node.next = self.head.next
        self.head.next.prev = node
        self.head.next = node
    
    def _remove_node(self, node):
        node.prev.next = node.next
        node.next.prev = node.prev
    
    def get(self, key):
        if key not in self.cache:
            return -1
        node = self.cache[key]
        self._remove_node(node)
        self._add_to_head(node)
        return node.value
    
    def put(self, key, value):
        if key in self.cache:
            node = self.cache[key]
            node.value = value
            self._remove_node(node)
            self._add_to_head(node)
        else:
            node = Node(key, value)
            self.cache[key] = node
            self._add_to_head(node)
            if len(self.cache) > self.capacity:
                lru = self.tail.prev
                self._remove_node(lru)
                del self.cache[lru.key]
```

### LFU缓存 (LFU Cache)
**核心思路**: 哈希表 + 频率哈希表 + 双向链表。

```python
class LFUCache:
    def __init__(self, capacity):
        self.capacity = capacity
        self.min_freq = 0
        self.key_to_val = {}
        self.key_to_freq = {}
        self.freq_to_keys = {}
    
    def _update_freq(self, key):
        freq = self.key_to_freq[key]
        self.key_to_freq[key] = freq + 1
        self.freq_to_keys[freq].remove(key)
        
        if not self.freq_to_keys[freq]:
            del self.freq_to_keys[freq]
            if freq == self.min_freq:
                self.min_freq += 1
        
        self.freq_to_keys.setdefault(freq + 1, []).append(key)
    
    def get(self, key):
        if key not in self.key_to_val:
            return -1
        self._update_freq(key)
        return self.key_to_val[key]
    
    def put(self, key, value):
        if self.capacity == 0:
            return
        
        if key in self.key_to_val:
            self.key_to_val[key] = value
            self._update_freq(key)
        else:
            if len(self.key_to_val) >= self.capacity:
                evict_key = self.freq_to_keys[self.min_freq].pop(0)
                del self.key_to_val[evict_key]
                del self.key_to_freq[evict_key]
            
            self.key_to_val[key] = value
            self.key_to_freq[key] = 1
            self.freq_to_keys.setdefault(1, []).append(key)
            self.min_freq = 1
```

---

## 22. 矩阵问题 (Matrix)

### 螺旋矩阵 (Spiral Matrix)
**核心思路**: 按层遍历，维护上下左右边界。

```python
def spiralOrder(matrix):
    if not matrix:
        return []
    
    result = []
    top, bottom = 0, len(matrix) - 1
    left, right = 0, len(matrix[0]) - 1
    
    while top <= bottom and left <= right:
        # 从左到右
        for col in range(left, right + 1):
            result.append(matrix[top][col])
        top += 1
        
        # 从上到下
        for row in range(top, bottom + 1):
            result.append(matrix[row][right])
        right -= 1
        
        if top <= bottom:
            # 从右到左
            for col in range(right, left - 1, -1):
                result.append(matrix[bottom][col])
            bottom -= 1
        
        if left <= right:
            # 从下到上
            for row in range(bottom, top - 1, -1):
                result.append(matrix[row][left])
            left += 1
    
    return result
```

### 旋转图像 (Rotate Image)
**核心思路**: 先转置，再水平翻转（或先水平翻转，再转置）。

```python
def rotate(matrix):
    n = len(matrix)
    
    # 转置
    for i in range(n):
        for j in range(i, n):
            matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]
    
    # 水平翻转
    for i in range(n):
        matrix[i].reverse()
```

### 搜索二维矩阵 II (Search a 2D Matrix II)
**核心思路**: 从右上角或左下角开始搜索。

```python
def searchMatrix(matrix, target):
    if not matrix or not matrix[0]:
        return False
    
    row, col = 0, len(matrix[0]) - 1
    
    while row < len(matrix) and col >= 0:
        if matrix[row][col] == target:
            return True
        elif matrix[row][col] > target:
            col -= 1
        else:
            row += 1
    
    return False
```

---

## 总结

以上是 LeetCode 中最经典、最常考的题型分类和核心解法。建议按照以下顺序刷题：

1. **基础数据结构**: 数组、链表、字符串、栈、队列
2. **进阶算法**: 二分查找、双指针、滑动窗口
3. **树和图**: 各种遍历、DFS/BFS
4. **动态规划**: 从简单到复杂
5. **回溯算法**: 排列组合问题
6. **高级技巧**: 位运算、数学问题、堆

每个题型都要理解核心思想，多做几道同类题目，举一反三！
