# 位置编码


前置问题需要了解
- 位置编码里的相对位置编码和绝对位置编码的经典方案，做法  ，各自的优势是什么
- 以 此引出旋转位置编码的动机
- 基于其，开始推导一个期望的数学公式，即  fm  qn = 可以是一个关于 (m-n)的表达式
- 二维rope引申到多维rope
- rope两个参数：位置m和角度theta,   旋转角度是 m . theta, 带来的量程和精度问题，不同的位置，m是绝对位置，theta是单个向量内的位置，theta如果采用统一的值，那么会出现theta过小，相邻绝对位置的token旋转幅度就会偏小（即精度丢失） ，过大的话，那么很容易出现相同旋转值，模型对绝对位置的感知能力就会弱（ 即量程） ， 因此theta的取值不是单个向量里完全一致的，而是采取不同的取值设定， 其实际是一个单调递减的函数函数，可以理解为不同位置的向量其变化幅度不一致，越靠前的变化幅度很大，越靠后的位置变化幅度越小（ 类比为进制，个位数疯狂跳动，万位变化幅度很慢） 
- rope的特性，自带远程衰减（ 符合语言模型特质） --> 分析方案，假设q 和 k相同，直接计算其带有位置向量后的attention值； 计算与 q0 与 k0-kn的分数， 计算 qn与 k0- kn的值，
- rope的特性： 外推性： 推理时候修改theta，假设原本的最大训练长度 是m, 那么只要插值使得 2m k theta = 原本的 m theta ，



# 模型架构

## qwen3 next 的，delta 门控

## moe和稠密模型

## 权重复用： gqa, mhq, 以及layer级别的权重复用（ q级别，qkv级别，layer级别等）

权重复用大量减少 kv cache ，

适用于小模型，对于大模型来说，一般不会采用这些方案， 细粒度的感知仍然是大模型做出复杂决策 推理能力 的关键




# 编码器 解码器


## 为什么decoder only 好于 encoder only

我觉得可以举一个例子， 小明
